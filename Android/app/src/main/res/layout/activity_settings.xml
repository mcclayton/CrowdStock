<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.crowdstock.app.main.SettingsActivity">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/logoImageView"
        android:src="@drawable/crowdstock_logo"
        android:paddingBottom="100dp"
        android:contentDescription="@string/desc"
        android:layout_centerHorizontal="true" />

    <ToggleButton
        android:layout_width="75dp"
        android:layout_height="75dp"
        android:id="@+id/toggleSettingA"
        android:layout_below="@+id/logoImageView"
        android:hint="@string/settingA" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/toggleSettingAText"
        android:text="@string/settingA"
        android:paddingBottom="20dp"
        android:layout_above="@+id/toggleSettingA" />

</RelativeLayout>
